@model Muesli.ViewModels.Cart

<div class="navbar-text">
    <i class="fa fa-shopping-cart" aria-hidden="true" data-toggle="modal" data-target="#exampleModal"><span class="badge badge-light">@Model.Lines.Count</span></i>


    @*@if (Model.Lines.Count == 0)
        {
            <b>Your cart is empty</b>
        }
        else
        {
            <b>Your cart:</b>
            @Model.Lines.Count <span>item(s),</span>
            <span>€ </span>@Model.TotalPrice.ToString("n")
        }*@
    @*<input id="btnAddToCart" type="submit" class="btn btn-primary " data-toggle="modal" data-target="#exampleModal" value="Cart" />*@

</div>
<!--<button id="btnAddToCart" type="submit">+ SHOW</button>-->
<!-- Button trigger modal -->
<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Your cart</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <table class="table">
                    <thead>
                        <tr>
                            <th class="text-left">Item</th>
                            <th class="text-right">Price per gram</th>
                            <th class="text-right">Quantity</th>
                            <th class="text-right">Subtotal</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var line in Model.Lines)

            {
                            <tr>
                                <td class="text-left">@line.Ingredient.Name</td>
                                <td class="text-right">€ @line.Ingredient.Price.ToString("n")</td>
                                <td class="text-right">@line.Quantity<span> g</span></td>
                                <td class="text-right">€ @((line.Quantity * line.Ingredient.Price).ToString("n"))</td>
                                <td>
                                    @using (Html.BeginForm("RemoveFromCart", "Cart"))
                {
                                        @Html.Hidden("IngredientId", line.Ingredient.IngredientId)
                                        <input class="btn btn-sm btn-warning" type="submit" value="Remove" />
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="2" class="text-right">Total:</td>
                            <td class="text-right">
                                @Model.TotalWeight<span> g</span>
                            </td>
                            <td class="text-right">
                                € @Model.TotalPrice.ToString("n")
                            </td>
                        </tr>
                    </tfoot>
                </table>

            </div>
            <div class="modal-footer">
                <input id="continue-shopping" class="btn btn-secondary" data-dismiss="modal" value="Continue shopping" />
                @Html.ActionLink("Go to Cart", "Index", null, new { @class = "btn btn-primary" })
            </div>
        </div>
    </div>
</div>

<script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.3/umd/popper.min.js" integrity="sha384-vFJXuSJphROIrBnz7yo7oB41mKfc8JzQZiCq4NCceLEaO4IHwicKwpJf9c9IpFgh" crossorigin="anonymous"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.2/js/bootstrap.min.js" integrity="sha384-alpBpkh1PFOepccYVYDB4do5UnbKysX5WZXm3XxPqe5iKTfUKjNkCk9SaVuEZflJ" crossorigin="anonymous"></script>

