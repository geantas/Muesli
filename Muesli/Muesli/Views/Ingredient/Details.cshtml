@model Muesli.Models.Ingredient

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Ingredient</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            Category <!--@Html.DisplayNameFor(model => model.Ingredient_Categories) -->
        </dt>
        <dd>
            @foreach (var item in Model.Ingredient_Categories)
            {
                @Html.DisplayFor(modelItem => item.Category.CatName)
            }
        </dd>


        <dt>
            @Html.DisplayNameFor(model => model.Price)
        </dt>

        <dd>
            €@Html.DisplayFor(model => model.Price)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.IngredientId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
